# Existing secrets example
#
# This example demonstrates how to use existing Kubernetes secrets with the Home Information chart.

config:
  existingSecretName: my-django-secret
  existingSecretKey: secret-key
  auth:
    adminEmail: admin@example.com
    existingSecret: my-auth-secret
    existingSecretKey: DJANGO_SUPERUSER_PASSWORD

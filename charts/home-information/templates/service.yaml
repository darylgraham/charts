apiVersion: v1
kind: Service
metadata:
  name: {{ include "home-information.fullname" . }}
  labels:
    {{- include "home-information.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "home-information.selectorLabels" . | nindent 4 }}
